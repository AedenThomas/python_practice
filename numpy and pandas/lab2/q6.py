# Create a numpy array to print all zeros and dtype as a float

import numpy as np

# Create an array of 5 elements and set the data type to float
arr = np.zeros(5, dtype=float)

# Print the array
print(arr)
